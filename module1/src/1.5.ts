// reference type -->> Object

const user: {
  bekar: "hae"; // type --> litarel types
  readonly JustPoro: string; // that can be only read not modified
  firstName: string;
  middleName: string;
  LastName?: string; // optional types define by ? Mark
  isMarrid?: boolean; // optional types define by ? Mark
} = {
  JustPoro: "kochu porih",
  bekar: "hae",
  firstName: "Md",
  middleName: "Morsaline",
  isMarrid: false,
};
